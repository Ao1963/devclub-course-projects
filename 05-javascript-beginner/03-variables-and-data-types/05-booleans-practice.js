const isHungry = true;
const isTired = false;

console.log(`Is the student hungry? ${isHungry}`);
console.log(`Is the student tired? ${isTired}`);

/* 
   LOG: Booleans are like light switches. 
   They are essential for conditional logic (if/else).
*/

const userAge = 62;
const canDrive = userAge >= 18; // Isso gera um Boolean automaticamente

console.log(`Can the user drive? ${canDrive}`); 

/* 
   LOG: The variable 'canDrive' is a Boolean result of a mathematical comparison.
   This is how we automate decisions in real applications.
*/
